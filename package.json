{
  "name": "mis",
  "version": "1.0.0",
  "description": "a full stack node app for managing Friends of the Petaluma Library membership data",
  "keywords": [],
  "license": "reserved",
  "scripts": {
    "start": "pnpm --stream -r start",
    "test": "pnpm --stream -r test",
    "test:dev": "pnpm --stream -r test:dev",
    "start:dev": "scripts/start-db; pnpm --stream -r start:dev",
    "backupdb": "pnpm --stream -r backupdb"
  },
  "dependencies": {
    "eslint-config-standard-with-typescript": "^39.1.0",
    "pnmp": "^1.0.4"
  },
  "devDependencies": {
    "create-nightwatch": "3.0.2",
    "jscpd": "^3.5.10"
  }
}